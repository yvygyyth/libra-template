/**
 * Vitest 测试环境设置文件
 * 用于在运行测试前进行全局配置和初始化
 */

// 可以在这里添加全局测试设置，例如：
// - 全局 mock
// - 测试环境变量
// - 全局 beforeEach/afterEach 钩子
// - 自定义匹配器

// 示例：如果需要全局 mock，可以在这里添加
// vi.mock('some-module', () => ({
//   default: {
//     someMethod: vi.fn()
//   }
// }))
